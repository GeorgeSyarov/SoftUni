function info(name,pop,area){
    name = String(name);
    pop = Number(pop);
    area = Number(area);
    console.log(`Town ${name} has population of ${pop} and area ${area} square km.`);
}
info('Sofia',128383,492);